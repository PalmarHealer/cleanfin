name: Build CSS

on:
  push:
    branches: [main]
    paths:
      - '**.scss'
      - '.github/workflows/build.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Sass
        run: npm install -g sass

      - name: Compile expanded CSS
        run: sass --no-source-map --style=expanded style.scss style.css

      - name: Compile minified CSS
        run: sass --no-source-map --style=compressed style.scss style.min.css

      - name: Commit built files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add style.css style.min.css
          if git diff --staged --quiet; then
            echo "No changes to commit."
          else
            git commit -m "build: compile SCSS to CSS"
            git push
          fi
